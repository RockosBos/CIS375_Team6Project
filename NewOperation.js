//This page will be used to generate HTML and will work with both the Operation class and index files
function addRow(){
    let Element = document.createElement("inputRow");
    let operationText = document.createTextNode("Test");

    Element.appendChild(operationText);
    var inputDiv = document.getElementById("Inputs"); 
    document.


}